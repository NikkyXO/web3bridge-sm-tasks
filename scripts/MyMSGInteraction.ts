// import { ethers } from "hardhat";

// import addresses from "../ignition/deployments/"

// const MyMSGWallet = await ethers.getContractAt("MyMSGWallet", walletAddress);

// // Initiate a transfer transaction
// await MyMSGWallet.connect(signer1).initiateTransactionForTransfer(ethers.utils.parseEther('1'), recipient.address, tokenAddress);

// // Approve a transaction
// await MyMSGWallet.connect(signer2).approveTx(1);
